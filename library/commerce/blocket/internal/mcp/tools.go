// Copyright 2026 johan-wallmn. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	"github.com/mvanhorn/printing-press-library/library/commerce/blocket/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/commerce/blocket/internal/client"
	"github.com/mvanhorn/printing-press-library/library/commerce/blocket/internal/config"
	"github.com/mvanhorn/printing-press-library/library/commerce/blocket/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/commerce/blocket/internal/store"
	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("ads_get",
			mcplib.WithDescription("Fetch full detail for a BAP listing. Required: ad_id. Returns the AdDetail."),
			mcplib.WithString("ad_id", mcplib.Required(), mcplib.Description("Blocket ad id.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/recommerce/forsale/item/{ad_id}", []mcpParamBinding{{PublicName: "ad_id", WireName: "ad_id", Location: "path"}}, []string{"ad_id"}),
	)
	s.AddTool(
		mcplib.NewTool("ads_list",
			mcplib.WithDescription("List general BAP listings (SEARCH_ID_BAP_COMMON). Optional: query, category, location (plus 10 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query against listing headings.")),
			mcplib.WithString("category", mcplib.Description("Hierarchical category id (e.g. 0.91, 1.91.3108, 2.91.3108.362).")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id; repeat by comma-separating values.")),
			mcplib.WithString("radius", mcplib.Description("Radius in km when combined with a location seed.")),
			mcplib.WithString("trade_type", mcplib.Description("Seller intent — Säljes (selling), Köpes (buying), Bortskänkes (giving away).")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price in SEK.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price in SEK.")),
			mcplib.WithString("dealer_segment", mcplib.Description("Filter by dealer segment (private vs dealer).")),
			mcplib.WithString("condition", mcplib.Description("Listing condition — new, used, broken, etc.")),
			mcplib.WithString("shipping_types", mcplib.Description("Filter by shipping option (e.g. with-shipping, pickup-only).")),
			mcplib.WithString("published", mcplib.Description("Recency filter (e.g. last_24h, last_week).")),
			mcplib.WithString("sort", mcplib.Description("Sort order (default newest).")),
			mcplib.WithString("page", mcplib.Description("Page number (1-based, 50 results per page).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/recommerce/forsale/search/api/search/SEARCH_ID_BAP_COMMON", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "category", WireName: "category", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "radius", WireName: "radius", Location: "query"}, {PublicName: "trade_type", WireName: "trade_type", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "dealer_segment", WireName: "dealer_segment", Location: "query"}, {PublicName: "condition", WireName: "condition", Location: "query"}, {PublicName: "shipping_types", WireName: "shipping_types", Location: "query"}, {PublicName: "published", WireName: "published", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("boats_list",
			mcplib.WithDescription("List used-boat listings. Optional: query, type, location (plus 7 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("type", mcplib.Description("Boat type (Motorbåt, Segelbåt, Roddbåt, …).")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("length_from", mcplib.Description("Minimum length in feet.")),
			mcplib.WithString("length_to", mcplib.Description("Maximum length in feet.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price in SEK.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price in SEK.")),
			mcplib.WithString("org_id", mcplib.Description("Restrict to dealer org_id.")),
			mcplib.WithString("sort", mcplib.Description("Sort order.")),
			mcplib.WithString("page", mcplib.Description("Page number (1-based).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_BOAT_USED", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "length_from", WireName: "length_from", Location: "query"}, {PublicName: "length_to", WireName: "length_to", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "org_id", WireName: "org_id", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("cars_get",
			mcplib.WithDescription("Fetch full detail for a vehicle listing. Required: ad_id. Returns the AdDetail."),
			mcplib.WithString("ad_id", mcplib.Required(), mcplib.Description("Blocket ad id.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/item/{ad_id}", []mcpParamBinding{{PublicName: "ad_id", WireName: "ad_id", Location: "path"}}, []string{"ad_id"}),
	)
	s.AddTool(
		mcplib.NewTool("cars_list",
			mcplib.WithDescription("List used-car listings. Optional: query, make, model (plus 22 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("make", mcplib.Description("Vehicle make (Volvo, Saab, BMW, …); repeat for OR.")),
			mcplib.WithString("model", mcplib.Description("Vehicle model (XC70, V70, …); repeat for OR.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("radius", mcplib.Description("Radius in km when combined with a location seed.")),
			mcplib.WithString("fuel", mcplib.Description("Fuel type (Bensin, Diesel, El, Hybrid, …).")),
			mcplib.WithString("body_type", mcplib.Description("Body type (Kombi, Sedan, SUV, …).")),
			mcplib.WithString("sales_form", mcplib.Description("Sales form (Säljes, Bytes, Hyrköp, …).")),
			mcplib.WithString("dealer_segment", mcplib.Description("Private vs dealer.")),
			mcplib.WithString("transmission", mcplib.Description("Manuell or Automat.")),
			mcplib.WithString("wheel_drive", mcplib.Description("Drivetrain (FWD, RWD, AWD, 4WD).")),
			mcplib.WithString("exterior_colour", mcplib.Description("Exterior colour.")),
			mcplib.WithString("vat_deductible", mcplib.Description("Filter for VAT-deductible listings only.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price in SEK.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price in SEK.")),
			mcplib.WithString("year_from", mcplib.Description("Minimum model year.")),
			mcplib.WithString("year_to", mcplib.Description("Maximum model year.")),
			mcplib.WithString("mileage_from", mcplib.Description("Minimum mileage in mil (Swedish miles, 1 mil = 10 km).")),
			mcplib.WithString("mileage_to", mcplib.Description("Maximum mileage in mil.")),
			mcplib.WithString("engine_effect", mcplib.Description("Engine power (horsepower band).")),
			mcplib.WithString("car_equipment", mcplib.Description("Equipment filter (e.g. parkeringssensor, dragkrok).")),
			mcplib.WithString("org_id", mcplib.Description("Restrict to a specific dealer org_id.")),
			mcplib.WithString("published", mcplib.Description("Recency filter.")),
			mcplib.WithString("sort", mcplib.Description("Sort order.")),
			mcplib.WithString("page", mcplib.Description("Page number (1-based).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_CAR_USED", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "make", WireName: "make", Location: "query"}, {PublicName: "model", WireName: "model", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "radius", WireName: "radius", Location: "query"}, {PublicName: "fuel", WireName: "fuel", Location: "query"}, {PublicName: "body_type", WireName: "body_type", Location: "query"}, {PublicName: "sales_form", WireName: "sales_form", Location: "query"}, {PublicName: "dealer_segment", WireName: "dealer_segment", Location: "query"}, {PublicName: "transmission", WireName: "transmission", Location: "query"}, {PublicName: "wheel_drive", WireName: "wheel_drive", Location: "query"}, {PublicName: "exterior_colour", WireName: "exterior_colour", Location: "query"}, {PublicName: "vat_deductible", WireName: "vat_deductible", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "year_from", WireName: "year_from", Location: "query"}, {PublicName: "year_to", WireName: "year_to", Location: "query"}, {PublicName: "mileage_from", WireName: "mileage_from", Location: "query"}, {PublicName: "mileage_to", WireName: "mileage_to", Location: "query"}, {PublicName: "engine_effect", WireName: "engine_effect", Location: "query"}, {PublicName: "car_equipment", WireName: "car_equipment", Location: "query"}, {PublicName: "org_id", WireName: "org_id", Location: "query"}, {PublicName: "published", WireName: "published", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_atractors",
			mcplib.WithDescription("A-tractors / EPA-traktors (SEARCH_ID_CAR_A_TRACTOR). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_CAR_A_TRACTOR", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_atvs",
			mcplib.WithDescription("ATVs / fyrhjulingar (SEARCH_ID_MC_ATV). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_MC_ATV", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_buses",
			mcplib.WithDescription("Buses (SEARCH_ID_CAR_BUS). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_CAR_BUS", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_caravans",
			mcplib.WithDescription("Caravans (SEARCH_ID_CAR_CARAVAN). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_CAR_CARAVAN", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_combines",
			mcplib.WithDescription("Agriculture combines / threshing machines (SEARCH_ID_AGRICULTURE_THRESHING). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_AGRICULTURE_THRESHING", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_construction",
			mcplib.WithDescription("Construction equipment (SEARCH_ID_CAR_AGRI). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_CAR_AGRI", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_mobilehomes",
			mcplib.WithDescription("Mobile homes (SEARCH_ID_CAR_MOBILE_HOME). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_CAR_MOBILE_HOME", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_scooters",
			mcplib.WithDescription("Scooters (SEARCH_ID_MC_SCOOTER). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_MC_SCOOTER", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_tools",
			mcplib.WithDescription("Agriculture tools (SEARCH_ID_AGRICULTURE_TOOL). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_AGRICULTURE_TOOL", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_tractors",
			mcplib.WithDescription("Agriculture tractors (SEARCH_ID_AGRICULTURE_TRACTOR). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_AGRICULTURE_TRACTOR", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("mobility_trucks",
			mcplib.WithDescription("Heavy trucks (SEARCH_ID_CAR_TRUCK). Optional: query, location, price_from (plus 2 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price.")),
			mcplib.WithString("page", mcplib.Description("Page number.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_CAR_TRUCK", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("motorcycles_list",
			mcplib.WithDescription("List used-motorcycle listings. Optional: query, type, model (plus 8 more). Returns the SearchResponse."),
			mcplib.WithString("query", mcplib.Description("Free-text query.")),
			mcplib.WithString("type", mcplib.Description("Motorcycle type (Sport, Touring, Cruiser, Enduro, …).")),
			mcplib.WithString("model", mcplib.Description("Specific model.")),
			mcplib.WithString("location", mcplib.Description("Swedish region/county id.")),
			mcplib.WithString("price_from", mcplib.Description("Minimum price in SEK.")),
			mcplib.WithString("price_to", mcplib.Description("Maximum price in SEK.")),
			mcplib.WithString("engine_volume_from", mcplib.Description("Minimum engine volume in cc.")),
			mcplib.WithString("engine_volume_to", mcplib.Description("Maximum engine volume in cc.")),
			mcplib.WithString("org_id", mcplib.Description("Restrict to dealer org_id.")),
			mcplib.WithString("sort", mcplib.Description("Sort order.")),
			mcplib.WithString("page", mcplib.Description("Page number (1-based).")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/mobility/search/api/search/SEARCH_ID_MC_USED", []mcpParamBinding{{PublicName: "query", WireName: "q", Location: "query"}, {PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "model", WireName: "model", Location: "query"}, {PublicName: "location", WireName: "location", Location: "query"}, {PublicName: "price_from", WireName: "price_from", Location: "query"}, {PublicName: "price_to", WireName: "price_to", Location: "query"}, {PublicName: "engine_volume_from", WireName: "engine_volume_from", Location: "query"}, {PublicName: "engine_volume_to", WireName: "engine_volume_to", Location: "query"}, {PublicName: "org_id", WireName: "org_id", Location: "query"}, {PublicName: "sort", WireName: "sort", Location: "query"}, {PublicName: "page", WireName: "page", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + msg +
					"\nhint: check your API credentials." +
					"\n      Run 'blocket-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + msg +
					"\nhint: this API is configured without credentials; the service may be blocking the request by rate limit, geography, bot protection, or endpoint policy." +
					"\n      Run 'blocket-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "blocket-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 2)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "blocket-pp-cli", "data.db")
}

// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "blocket",
		"description": "Search Blocket and its mobility verticals from the terminal, with a local store, price history, and arbitrage detection.",
		"archetype":   "generic",
		"tool_count":  17,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion blocket-pp-cli binary.",
		"resources": []map[string]any{
			{
				"name":        "ads",
				"description": "Browse and fetch general BAP (Buying And Selling) listings — everything that is not a vehicle.",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "boats",
				"description": "Search used-boat listings (SEARCH_ID_BOAT_USED).",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "cars",
				"description": "Search and fetch used-car listings (SEARCH_ID_CAR_USED).",
				"endpoints":   []string{"get", "list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "mobility",
				"description": "Niche mobility verticals — trucks, buses, construction, agriculture, caravans, mobile homes, A-traktor, ATVs,...",
				"endpoints":   []string{"atractors", "atvs", "buses", "caravans", "combines", "construction", "mobilehomes", "scooters", "tools", "tractors", "trucks"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "motorcycles",
				"description": "Search used-motorcycle listings (SEARCH_ID_MC_USED).",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "What's new since timestamp", "command": "since", "description": "List ads added since a timestamp for any stored named-search, with cron-friendly exit codes when matches are found.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Arbitrage / underpriced detector", "command": "arbitrage", "description": "Find current listings priced below threshold × median for their (make, model, year-band, mileage-band) cohort.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Price history per ad", "command": "price-history", "description": "Show every snapshotted price for an ad over time with deltas; sync populates `ad_price_snapshots` on each run.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Dealer portfolio with stats", "command": "dealer ads", "description": "Aggregate every current ad for an `org_id` plus inventory count, median price, oldest-listing-days, and percent...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Stale listings detection", "command": "stale", "description": "Listings older than N days that are still active in the most recent sync, optionally filtered by vertical or dealer.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Cross-vertical search", "command": "search-all", "description": "Free-text search that fans across BAP, used cars, boats, motorcycles, and the ten niche mobility verticals locally...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Watch with price-drop notification", "command": "watch run", "description": "Run a stored named-search and exit non-zero with structured JSON when ads in the result set dropped in price since...", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Description regex", "command": "desc-grep", "description": "Match a Go regular expression against ad description text backfilled by ad-detail sync.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Geo radius search", "command": "geo near", "description": "Filter synced ads by point + radius using haversine distance against `coordinates`.", "rationale": "", "via": "mcp-command-mirror"},
			{"name": "Vehicle appraisal", "command": "appraise", "description": "Given a make/model/year/mileage, compute p10/p50/p90 of asking prices from the synced corpus and render the...", "rationale": "", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "What's new since timestamp", "insight": ""},
			{"topic": "Arbitrage / underpriced detector", "insight": ""},
			{"topic": "Price history per ad", "insight": ""},
			{"topic": "Dealer portfolio with stats", "insight": ""},
			{"topic": "Stale listings detection", "insight": ""},
			{"topic": "Cross-vertical search", "insight": ""},
			{"topic": "Watch with price-drop notification", "insight": ""},
			{"topic": "Description regex", "insight": ""},
			{"topic": "Geo radius search", "insight": ""},
			{"topic": "Vehicle appraisal", "insight": ""},
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
