// Copyright 2026 johan-wallmn. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMotorcyclesPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagQ string
	var flagType string
	var flagModel string
	var flagLocation string
	var flagPriceFrom int
	var flagPriceTo int
	var flagEngineVolumeFrom int
	var flagEngineVolumeTo int
	var flagOrgId string
	var flagSort string
	var flagPage string

	cmd := &cobra.Command{
		Use:         "motorcycles",
		Short:       "List used-motorcycle listings.",
		Long:        "Shortcut for 'motorcycles list'. List used-motorcycle listings.",
		Example:     "  blocket-pp-cli motorcycles",
		Annotations: map[string]string{"pp:endpoint": "motorcycles.list", "pp:method": "GET", "pp:path": "/mobility/search/api/search/SEARCH_ID_MC_USED", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/mobility/search/api/search/SEARCH_ID_MC_USED"
			params := map[string]string{}
			if flagQ != "" {
				params["q"] = fmt.Sprintf("%v", flagQ)
			}
			if flagType != "" {
				params["type"] = fmt.Sprintf("%v", flagType)
			}
			if flagModel != "" {
				params["model"] = fmt.Sprintf("%v", flagModel)
			}
			if flagLocation != "" {
				params["location"] = fmt.Sprintf("%v", flagLocation)
			}
			if flagPriceFrom != 0 {
				params["price_from"] = fmt.Sprintf("%v", flagPriceFrom)
			}
			if flagPriceTo != 0 {
				params["price_to"] = fmt.Sprintf("%v", flagPriceTo)
			}
			if flagEngineVolumeFrom != 0 {
				params["engine_volume_from"] = fmt.Sprintf("%v", flagEngineVolumeFrom)
			}
			if flagEngineVolumeTo != 0 {
				params["engine_volume_to"] = fmt.Sprintf("%v", flagEngineVolumeTo)
			}
			if flagOrgId != "" {
				params["org_id"] = fmt.Sprintf("%v", flagOrgId)
			}
			if flagSort != "" {
				params["sort"] = fmt.Sprintf("%v", flagSort)
			}
			if flagPage != "" {
				params["page"] = fmt.Sprintf("%v", flagPage)
			}
			data, prov, err := resolveRead(cmd.Context(), c, flags, "motorcycles", false, path, params, nil)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagQ, "query", "", "Free-text query.")
	cmd.Flags().StringVar(&flagType, "type", "", "Motorcycle type (Sport, Touring, Cruiser, Enduro, …).")
	cmd.Flags().StringVar(&flagModel, "model", "", "Specific model.")
	cmd.Flags().StringVar(&flagLocation, "location", "", "Swedish region/county id.")
	cmd.Flags().IntVar(&flagPriceFrom, "price-from", 0, "Minimum price in SEK.")
	cmd.Flags().IntVar(&flagPriceTo, "price-to", 0, "Maximum price in SEK.")
	cmd.Flags().IntVar(&flagEngineVolumeFrom, "engine-volume-from", 0, "Minimum engine volume in cc.")
	cmd.Flags().IntVar(&flagEngineVolumeTo, "engine-volume-to", 0, "Maximum engine volume in cc.")
	cmd.Flags().StringVar(&flagOrgId, "org-id", "", "Restrict to dealer org_id.")
	cmd.Flags().StringVar(&flagSort, "sort", "PUBLISHED_DESC", "Sort order.")
	cmd.Flags().StringVar(&flagPage, "page", "1", "Page number (1-based).")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
